# AGENTS.md — 根规则

> 本仓库的根规则（维护场景定义：通用必须文档）。规则变更时同步更新。

## 全局规则

- 所有操作：先告知理解，用户确认后再执行
- 优先级：用户拍板 > 本文件 > 域内规则 > 场景规则
- 冲突发现：记录 → 请用户裁决 → 更新规则
- 先查证，不假设
- 不确定：读文档 / 实测 / 问用户
- 禁止凭空推断
- 禁止假设性结论
- 最优解：基于真实数据模拟推导
- 沉默 ≠ 同意
- 关键点：显式确认
- 变更：显式记录，禁止静默漂移
- 阶段推进：由用户决定，AI 只提示
- 禁止自行"假定完成"
- 记录落盘：每 2-3 轮一次
- 文档不锁死：用户可改
- 验证失败：修复 → 复验 → 用户确认
- 禁止带病进入下一阶段
- 删除/移动/重命名：先用户确认
- 记录：原路径 → 新路径 → 理由

## 语言与命名规则

- 文档名：只用英文
- 产物命名：按命名对照表
- 语言偏好：主 skill 识别，调用 zh / en
- en 镜像 zh：zh 变更后同步 en
- 同步时机：每次落盘

## 确认机制

- 复述：结构化重述，非复读
- 复述 → 用户确认 → 才对齐
- 逐条确认：已确认 / 待定 / 驳回
- 未确认项：不进下一阶段
- 驳回项：重新澄清再确认
- 记录来源：初稿提案 / 用户确认
