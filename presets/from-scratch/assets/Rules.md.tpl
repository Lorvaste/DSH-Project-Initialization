# Rules.md — 项目规范

> 本文件为项目的规则唯一来源（Single Source of Truth），包含：
> 语言偏好、上下文规则、开发规则、术语标准，以及**其他骨架的生成规则**。
> **访谈机制与准则不在此文件**——内化于场景模式 skill（interview）。
> 所有参与者（人类与 AI）默认遵守；规范变更需显式记录。

---

## 0. 骨架分工声明

| 部分 | 规则载体 | 说明 |
|---|---|---|
| **默认骨架**（通用 9+2 项）| **skill 内实现** | README/LICENSE/Rules/Structure/Progress/plan/Other/Reference/.git + .gitignore/.gitattributes；生成逻辑与默认内容固化在场景 scaffold skill 与 assets/ 模板中 |
| **其他骨架**（类型子结构）| **本文件（Rules.md）** | 软件/硬件/复合子结构的生成规则见第四章，skill 执行时读取本文件按规则生成 |
| **访谈规则**（机制与准则）| **场景模式 skill 指令** | 访谈机制、五组准则（提问/记录/收敛/冻结/角色）在场景 interview skill |

**项目内优先**：脚手架生成后，用户可修改本文件（含第四章规则）；skill 下次运行以项目内文件为准，不覆盖用户修改。

生成时序：默认骨架先建（skill 直接产出）→ 需求问答确定类型/规模 → 按第四章规则生成其他骨架。

---

## 1. 语言偏好

- 交流与文档默认中文；代码标识符、git 提交信息用英文
- 术语优先使用本文件「术语标准」中的定义
- 文档命名遵循项目结构规范（见 plan.md）

## 2. 上下文规则

- **需求变更必须显式记录**并更新规格文档，禁止静默漂移
- 关键理解点必须「AI 复述 → 用户确认」后才算对齐（规格冻结机制）
- **文档是唯一权威**：代码/产物与文档冲突时以文档为准，并记录偏差
- 产物对照发现的偏差必须生成偏差清单，逐项确认后修正
- 不满足当前阶段校验，不得进入下一阶段（流程状态机闸门）

## 3. 开发规则

- 遵循 **SDD**：先规格后实现
- 每个里程碑完成：文档保存 + git 提交
- 先搭骨架后填充：默认骨架 → 访谈 → 其他骨架（类型子结构）

### 3.1 git 规则

**初始化与身份**
- `git init -b main`（明确默认分支，与环境默认无关；已存在仓库跳过）
- 身份配置：项目级 `git config user.name/user.email`，勿改全局；初始提交前必须有可用身份

**分支**
- 主干即 `main`，不建长期并行分支（单会话流程）

**忽略与属性**
- `.gitignore` 必备：node_modules/dist/构建产物/编辑器/系统文件/密钥凭据（`.env`/`*.pem`/`*.key`/`*.pfx`/`*.p12`/`credentials*`/`id_rsa`/`id_ed25519`）/内部文档
- `.gitattributes`：统一 LF（`* text=auto` + 文档/代码扩展名 `eol=lf`），防 Windows CRLF 漂移

**提交**
- 提交规范：Conventional Commits——`<type>(<scope>): <subject>`，type ∈ `feat`/`fix`/`docs`/`chore`/`refactor`/`test`/`style`/`build`/`perf`
- 每个里程碑一个提交（脚手架、类型子结构、规格冻结、任务拆分、最终复核）
- 内部文件（开发进度/决策记录/访谈）不提交——git 例外（.gitignore 声明）

**推送**
- 场景结束（docs-push）才推送远端，push 可选
- ssh 最稳定；沙箱内 ssh 管道被拦时引导权限升级
- 推送前检查：无密钥、无内部文档、无未确认产物

**环境坑（条件式）**
- 若报 dubious ownership（Windows 常见：目录属主与当前用户不一致），用 `git -c safe.directory="<路径>" <cmd>` 逐命令绕过，勿改全局配置
- 远端可达确认：`git ls-remote <url>`

**安全**
- 密钥与凭据永不提交（`.env`/`*.pem`/`*.key`/`*.pfx`/`*.p12`/`*.jks`/`credentials*`/`id_rsa`/`id_ed25519`）
- 内部文档（开发日志/决策记录/访谈发现）git 例外，仅本地维护

---

## 4. 其他骨架生成规则（核心）

> 场景 skill 生成其他骨架时，以此章规则为唯一依据。

### 4.1 场景类型判定

需求问答结果决定场景类型：

| 判定条件 | 场景类型 |
|---|---|
| 有代码/逻辑产物，无物理产物 | **纯软件** |
| 有物理产物，无代码产物 | **纯实体** |
| 代码产物与物理产物都有 | **复合项目** |

判定记录：写入 Structure.md 与访谈记录；冻结时并入 spec.md 元信息（场景类型字段）。

### 4.2 软件子结构生成规则（有则生成）

| 子结构 | 生成条件 | 存放内容 |
|---|---|---|
| `Frontend/` | 有界面/交互层（网页、桌面、移动端 UI）| 前端相关文件与代码 |
| `Backend/` | 有服务端逻辑/API | 后端相关文件与代码 |
| `Database/` | 有数据持久化需求 | 数据库相关文件与代码 |

- 原则：访谈确认存在该功能块才生成，不满足不生成（小工具不建 Database）；按 4.2/4.3 逐项向用户确认
- 边界例句：固件内嵌 API 归 `Embedded/`，独立服务端才建 `Backend/`；持久化位置决定归属（设备存储→`Embedded/`，前端本地→`Frontend/`），独立数据库服务才建 `Database/`
- 功能块内部组织：按技术栈/模块子目录划分，由访谈确认

### 4.3 硬件子结构生成规则（有则生成，可扩展）

| 子结构 | 生成条件 | 存放内容 |
|---|---|---|
| `Model/` | 有三维模型/造型产物 | 模型相关文件 |
| `Electronic circuit/` | 有电路设计 | 电子电路相关文件 |
| `Embedded/` | 有固件/嵌入式代码 | 嵌入式开发相关文件 |
| `...` | 其他硬件功能块，按需扩展 | 自定义 |

- 原则：有对应硬件产物才生成；`...` 表示可扩展，扩展项由访谈确认后登记进本表
- 模型类项目内部组织可参考成熟规范（可复用库与项目专属分离，见 4.5）

### 4.4 复合项目组合规则

- 复合 = 软件子结构 + 硬件子结构并存，按类型与功能块拆分组合
- 功能块拆分原则：一个功能块一个文件夹；块间依赖最小化、边界清晰
- 示例：智能硬件项目 = `Frontend/` + `Backend/` + `Database/` + `Model/` + `Embedded/`
- **接口层归属**：复合项目跨软硬的数据协议/接口文档生成落点——`Reference/` 下（如 `Reference/protocol.md`），随子结构同步生成并在 Structure.md 登记

### 4.5 命名规则

- 子结构文件夹：固定命名（`Frontend`/`Backend`/`Database`/`Model`/`Electronic circuit`/`Embedded`）
- 项目专属模块：语义化命名，不缩写，不冗余
- 可复用资产与项目专属分离：如模型类项目 `Model/` 下分「通用可复用库」与「项目专属目录」，通用库沉淀由用户决定
- 变更命名需同步更新 Structure.md 与规格文档

### 4.6 结构追加规则（项目进行中路径）

- 类型变化（如纯软件新增硬件需求）：按 4.2/4.3 规则追加对应子结构，不重排已有结构
- 追加动作：生成子结构 → 更新 Structure.md → 更新规格文档 → 提交推送
- 偏差清单中「缺失」类偏差若涉及结构，按本条处理

### 4.7 生成时机

| 骨架 | 时机 |
|---|---|
| 默认骨架 | 流程早期：git init + ssh 远端（可选）之后 |
| 类型子结构 | 需求问答确定类型/规模之后、规格冻结之前 |

---

## 5. 术语标准

| 术语 | 定义 |
|---|---|
| SDD | 规格驱动开发：先写规格，再按规格实现 |
| 规格冻结 | AI 复述完整需求 → 用户逐条确认 → 冻结为权威文档 |
| 追溯矩阵 | 需求条目 ↔ 任务 ↔ 产物 ↔ 验收项的映射 |
| 默认骨架 | 通用结构：README/LICENSE/Rules/Structure/Progress/plan/Other/Reference/.git/.gitignore/.gitattributes（skill 内实现）|
| 其他骨架 | 类型子结构：软件（Frontend/Backend/Database）与硬件（Model/电路/嵌入式），按第四章规则有则生成 |
| 场景类型 | 纯软件 / 纯实体 / 复合项目（按 4.1 判定）|
| 功能域 | 用户理解的产品功能划分（问答④确认，MOD-xx）|
| 偏差清单 | 产物对照：符合/偏离/缺失 三类问题的逐项列表 |
| 对齐点 | 流程中显式检查理解一致性的环节（冻结、核对、验收）|
| 有则生成 | 访谈确认存在对应功能块才生成该子结构，不满足不生成 |
| git 例外 | 本地保留、不随公开仓库提交的文件（.gitignore 声明，如内部文档/密钥凭据）|
