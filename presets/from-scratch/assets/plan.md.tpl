# plan.md — 开发计划

> 本文件存放：项目结构规范（脚手架模板定义）、开发规划、验收规则。

---

## 第一部分：项目结构规范（脚手架模板）

### 两类进入阶段

1. **项目刚开始**
2. **项目进行中**

### 三种场景

1. 纯软件场景
2. 纯实体场景
3. 复合场景项目（软件+硬件）

### 项目刚开始的行为路径

```
确认工作区 → git 初始化 → ssh 链接远端仓库（用户可选，ssh 最稳定）
→ 初始化项目结构并保存（先通用骨架 9+2 项）
→ 进入需求问答阶段（确定类型/规模）
→ 生成类型子结构（软件/硬件，有则生成）
→ 整理统合需求并由用户确认（规格冻结）
→ 结构化编写文档并保存推送
```

### 项目进行中的行为路径

```
确认工作区 → 确认 git 状态与远端链接
→ 理解文档并由用户核对理解（复述确认）
→ 结构化重排文档 → 用户确认完成或者继续修正文档内容（循环）
→ 产物对照：AI 读取代码/文件，对照文档生成偏差清单 → 用户确认 → 逐项修正
```

### 共有结构（通用骨架，先建）

```
- README.md    自述文件：项目愿景、项目说明、文档目录
- LICENSE      开源协议
- Rules.md     项目规范：语言偏好、上下文规则、开发规则、术语标准
- Structure.md 项目内容：结构化信息
- Progress.md  项目进度
- plan.md      开发计划：开发规划、验收规则
- Other/       其他内容：决策思路等
- Reference/   参考文件（含冻结后规格文档 spec.md、任务拆分 tasks.md）
- .gitignore    git 忽略规则（node_modules/dist/系统文件等）
- .gitattributes  换行符与属性规范（统一 LF，防 Windows CRLF 漂移）
- .git/        git 相关文件
```

### 软件子结构（有则生成——有前端才建 Frontend，无数据库不建 Database）

```
- Frontend/   前端相关文件与代码
- Backend/    后端相关文件与代码
- Database/   数据库相关文件与代码
```

### 硬件子结构（有则生成，可扩展）

```
- Model/                模型相关文件
- Electronic circuit/   电子电路相关文件
- Embedded/             嵌入式开发相关文件
- ...
```

### 复合项目

按照类型与功能块拆分组合（软件子结构 + 硬件子结构）。

---

## 第二部分：开发规划（本项目）

（按 spec.md 功能域组织：阶段划分、里程碑、当前进度见 Progress.md）

## 遇到的错误（防重复失败）

| 错误 | 尝试次数 | 解决方案 |
|---|---|---|
| （记录执行中遇到的每个错误；同一错误不重复同样的失败操作）| | |

## 第三部分：验收规则（场景外，后续会话执行）

- 验收流程不在场景「从零开始」插件范围——执行/验收由后续会话基于 Reference/spec.md 与 Reference/tasks.md 进行
- 追溯依据：spec 条目 ID（F/D/R/I）与任务 ID（MOD-xx-Tyy）
