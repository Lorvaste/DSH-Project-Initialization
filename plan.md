# plan.md — 开发计划

> 本文件存放：项目结构规范（脚手架模板定义）、开发规划、验收规则。
> 本仓库自举此规范；插件脚手架的模板定义以此为准。

---

## 第一部分：项目结构规范（脚手架模板）

### 两类进入阶段

1. **项目刚开始**
2. **项目进行中**

### 三种场景

1. 纯软件场景
2. 纯实体场景
3. 复合场景项目（软件+硬件）

### 项目刚开始的行为路径

```
确认工作区 → git 初始化 → ssh 链接远端仓库（用户可选，ssh 最稳定）
→ 初始化项目结构并保存（先通用骨架 9 项）
→ 进入需求问答阶段（确定类型/规模）
→ 生成类型子结构（软件/硬件，有则生成）
→ 整理统合需求并由用户确认（规格冻结）
→ 结构化编写文档并保存推送
```

### 项目进行中的行为路径

```
确认工作区 → 确认 git 状态与远端链接
→ 理解文档并由用户核对理解（复述确认）
→ 结构化重排文档 → 用户确认完成或者继续修正文档内容（循环）
→ 产物对照：AI 读取代码/文件，对照文档生成偏差清单 → 用户确认 → 逐项修正
```

### 共有结构（通用骨架，先建）

```
- README.md    自述文件：项目愿景、项目说明、文档目录
- LICENSE      开源协议
- Rules.md     项目规范：语言偏好、上下文规则、开发规则、术语标准
- Structure.md 项目内容：结构化信息
- Progress.md  项目进度
- plan.md      开发计划：开发规划、验收规则
- Other/       其他内容：决策思路等
- Reference/   参考文件
- .gitignore    git 忽略规则（node_modules/dist/系统文件等）
- .gitattributes  换行符与属性规范（统一 LF，防 Windows CRLF 漂移）
- .git/        git 相关文件
```

### 软件子结构（有则生成——有前端才建 Frontend，无数据库不建 Database）

```
- Frontend/   前端相关文件与代码
- Backend/    后端相关文件与代码
- Database/   数据库相关文件与代码
```

### 硬件子结构（有则生成，可扩展）

```
- Model/                模型相关文件
- Electronic circuit/   电子电路相关文件
- Embedded/             嵌入式开发相关文件
- ...
```

### 复合项目

按照类型与功能块拆分组合（软件子结构 + 硬件子结构）。

---

## 第二部分：开发规划（本项目）

### 目标

开发 DSH 插件「AI 产品创建对齐」（场景模式），解决 AI 从零创建产品与想法不一致。
需求理解见 Reference/IDEA.md。

### 阶段

| 阶段 | 状态 | 说明 |
|---|---|---|
| 0. 需求澄清访谈 | complete | 5 轮访谈 + plan.md 规范 + 5 项裁决 |
| 1. 需求理解冻结 | in_progress | 确认 Reference/IDEA.md v0.2 后冻结 v1.0 |
| 2. 产品设计 | pending | 问答流程（六步）、规格结构、脚手架模板 |
| 3. 技术方案 | pending | 场景模式：工具白名单、skill 规范、流程状态机、插件打包 |
| 4. 插件实现 | pending | 按 MVP 边界实现 |
| 5. 验证 | pending | 本地安装测试、端到端从零流程 |
| 6. 开源分发 | pending | GitHub + 插件市场、文档 |

### MVP 边界（v1）

- 包含：从零流程（工作区/git+ssh/通用骨架/需求问答/类型子结构/规格冻结/文档推送）+ git 全配齐
- v1.1：任务分解+追溯、执行检查、软件验收对照
- v2：产物对照（进行中路径）、实体验收、复合场景补全

### 关键决策（23 条，详见 Reference/IDEA.md 第十章）

要点：三类场景两路径；先通用骨架后类型结构；软件/硬件有则生成；规格冻结=AI 复述+用户确认；git 全配齐含提交规范；从零优先纠偏并行；场景模式=白名单+skill+编排；分发双渠道。

### 待确认项（阻塞阶段 1）

1. 场景模式补充说明（用户未给出内容）
2. 用户额外需求（plan.md 已覆盖大部分，如有补充待给出）
3. 实体验收机制（量化验收/留接口/简化）
4. 插件与仓库命名

### 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---|---|
| （暂无）| | |

### 变更记录

- 2026-08：需求澄清 5 轮访谈 + plan.md 规范 + 5 项裁决；创建 IDEA.md v0.2（Reference/）；本仓库自举重组（README/Rules/Structure/Progress/plan/Other/Reference）

---

## 第三部分：验收规则（插件本身的验收标准）

### 功能验收

- [ ] 从零流程端到端可走通：工作区 → git init → ssh 远端（可选）→ 通用骨架 → 访谈 → 类型子结构 → 冻结 → 文档推送
- [ ] 规格冻结机制生效：AI 复述 → 用户逐条确认 → 不确认不冻结
- [ ] git 全配齐：init + 初始提交 + .gitignore + README + 提交规范
- [ ] 通用骨架 9 项完整生成，内容符合 plan.md 规范
- [ ] 类型子结构「有则生成」逻辑正确（小工具不建 Database）
- [ ] 进行中路径：文档对齐（复述核对）+ 产物对照（偏差清单）可走通

### 质量验收

- [ ] 工具白名单生效：访谈阶段仅挂载必要工具（省 token）
- [ ] 流程状态机：不满足当前阶段校验不得进入下一阶段
- [ ] 追溯矩阵：需求条目 ↔ 任务 ↔ 产物 ↔ 验收项 可追踪

### 分发验收

- [ ] 插件可在 DSH 本地安装
- [ ] 可在插件市场发布
- [ ] 文档齐全（README/使用说明/贡献指南）
